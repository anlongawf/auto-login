name: AutoLogin
version: '1.0.0'
main: com.cloudcheap.autologin.AutoLoginPlugin
api-version: '1.20'
folia-supported: true
description: Auto login players based on previous session.
authors: [AnPhan]

# Soft depend on AuthMe to ensure it loads after AuthMe if present
softdepend: [AuthMe]
